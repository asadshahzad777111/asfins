import { SITE } from "./site";

/** @deprecated Use SITE — kept for existing imports */
export const SHOP = {
  name: SITE.displayName,
  legalName: SITE.legalName,
  city: SITE.city,
  whatsapp: SITE.whatsapp,
  tagline: SITE.taglineEn,
  domain: SITE.domain,
  url: SITE.url,
  email: SITE.email,
};
